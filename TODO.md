# TODO — 优化与功能规划

本文档记录 TradingAgents-AShare 的优化方向和功能规划，分为"现有功能优化"、"新增功能"、"Agent 自主决策能力增强"和"系统性评估体系"四个部分。

---

## 一、现有功能优化

### 1. 跟踪看板 — 持仓排序与筛选

- **现状**：持仓列表按市值降序固定排列，无法自定义
- **方案**：
  - 支持按列头点击排序（涨跌幅、盈亏、市值等）
  - 支持按标的名称/代码搜索筛选
  - 排序偏好持久化到 localStorage
- **优先级**：P1
- **涉及文件**：`frontend/src/components/TrackingBoardPanel.tsx`

### 2. 跟踪看板 — 批量操作

- **现状**：删除只能逐个操作，没有批量选择
- **方案**：
  - 增加 checkbox 多选模式
  - 支持批量删除、批量触发分析
  - 全选/反选功能
- **优先级**：P1
- **涉及文件**：`frontend/src/components/TrackingBoardPanel.tsx`、`api/main.py`

### 3. 跟踪看板 — 实时行情状态指示

- **现状**：行情刷新间隔固定 20 秒，用户不知道数据是否实时
- **方案**：
  - 显示"最后更新时间"
  - 非交易时段显示"已休市"标识
  - 行情异常时（如数据源超时）显示警告
- **优先级**：P2
- **涉及文件**：`frontend/src/components/TrackingBoardPanel.tsx`

### 4. 持仓导入 — 格式兼容性增强

- **现状**：只支持 `代码 名称 持仓数 成本价 市值` 固定格式
- **方案**：
  - 支持更多券商导出格式（如通达信、同花顺、东方财富的导出 CSV）
  - 支持从 Excel/CSV 文件直接上传导入
  - 文本框增加格式示例和 placeholder 提示
- **优先级**：P1
- **涉及文件**：`api/main.py`、`api/services/portfolio_import_service.py`、`frontend/src/components/TrackingBoardPanel.tsx`

### 5. 控制台 — 持仓盈亏汇总

- **现状**：Dashboard 只显示跟踪看板摘要（标的数量）
- **方案**：
  - 增加总市值、总盈亏、总盈亏率的汇总卡片
  - 显示今日涨跌幅 TOP3 标的
  - 显示持仓分布饼图（按市值占比）
- **优先级**：P2
- **涉及文件**：`frontend/src/pages/Dashboard.tsx`、`api/main.py`

### 6. 全局 Toast 通知

- **现状**：各组件独立处理错误（有的用 alert、有的用 importFeedback）
- **方案**：统一使用全局 Toast 通知系统（成功/错误/警告），避免阻塞式 alert
- **优先级**：P1
- **涉及文件**：新增 `frontend/src/components/Toast.tsx`、`frontend/src/stores/toastStore.ts`

### 7. 前端性能 — 虚拟列表

- **现状**：持仓列表全量渲染，持仓数量多时可能卡顿
- **方案**：简洁版视图使用虚拟列表（如 react-virtuoso），只渲染可见区域
- **优先级**：P3
- **涉及文件**：`frontend/src/components/TrackingBoardPanel.tsx`

### 8. API 缓存 — 乐观更新

- **现状**：每次操作后都完整刷新看板数据
- **方案**：乐观更新（Optimistic Update），删除/追加后先更新本地状态，再异步同步
- **优先级**：P3
- **涉及文件**：`frontend/src/components/TrackingBoardPanel.tsx`

---

## 二、新增功能

### 9. 持仓预警系统

- **描述**：当持仓标的触发预设条件时推送通知
- **功能点**：
  - 价格突破/跌破预警（如跌破止损价、突破目标价）
  - 单日涨跌幅预警（如涨跌超过 5%）
  - 持仓盈亏预警（如浮亏超过 10%）
  - 推送方式：企业微信 Webhook（已有基础设施）
- **优先级**：P0
- **涉及文件**：
  - 后端：新增 `api/services/alert_service.py`、`api/models.py` 新增 Alert 模型
  - 前端：新增 `frontend/src/pages/Alerts.tsx`、预警设置组件
  - 定时检查：`api/scheduler/tasks.py` 新增预警检查任务

### 10. 持仓对比分析

- **描述**：将当前持仓与上一次分析报告的建议进行对比
- **功能点**：
  - 显示每只持仓标的最近一次分析的方向建议（买入/持有/卖出）
  - 标红"建议卖出但仍在持有"的标的
  - 标绿"建议买入但仓位不足"的标的
  - 一键触发"持仓体检"（批量分析所有持仓）
- **优先级**：P0
- **涉及文件**：
  - 后端：`api/services/tracking_board_service.py` 新增对比逻辑
  - 前端：`frontend/src/components/TrackingBoardPanel.tsx` 新增对比列

### 11. 持仓历史快照

- **描述**：记录每次持仓变更，支持回溯
- **功能点**：
  - 每次保存/追加/删除自动记录快照
  - 支持查看持仓变更历史时间线
  - 支持对比两个时间点的持仓差异
- **优先级**：P2
- **涉及文件**：
  - 后端：`api/models.py` 新增 PortfolioSnapshot 模型、`api/services/portfolio_import_service.py` 新增快照逻辑
  - 前端：新增持仓历史页面组件

### 12. 分析结果对比

- **描述**：同一标的多次分析结果的趋势对比
- **功能点**：
  - 显示同一标的历史分析的方向变化（如从"买入"→"持有"→"卖出"）
  - 置信度趋势图
  - 目标价/止损价变化趋势
- **优先级**：P2
- **涉及文件**：
  - 后端：`api/main.py` 新增分析历史趋势接口
  - 前端：`frontend/src/pages/Reports.tsx` 新增趋势对比视图

### 13. 交易日历集成

- **描述**：在定时分析和跟踪看板中集成交易日历
- **功能点**：
  - 显示今日是否为交易日
  - 显示下一个交易日日期
  - 非交易日自动跳过定时分析（已有部分逻辑，可完善 UI 提示）
- **优先级**：P2
- **涉及文件**：`api/services/scheduled_service.py`、`frontend/src/components/TrackingBoardPanel.tsx`

### 14. 持仓再平衡建议

- **描述**：基于分析结果自动给出仓位调整建议
- **功能点**：
  - 根据各标的的分析置信度和方向，计算建议仓位占比
  - 对比当前持仓占比，给出调仓建议（加仓/减仓/清仓）
  - 显示再平衡后的预期风险收益比
- **优先级**：P3
- **涉及文件**：
  - 后端：新增 `api/services/rebalance_service.py`
  - 前端：新增再平衡建议组件

---

## 三、Agent 自主决策能力增强

### 15. 方案 A：动态工具选择（Tool Selection）

- **核心思想**：让分析师根据初步分析结果，自主决定需要额外获取哪些数据
- **现状问题**：技术指标列表 `MARKET_INDICATORS` 硬编码，不会根据市场环境动态调整
- **方案设计**：
  - **两阶段分析流程**：
    - 阶段一（快速扫描）：用 Quick LLM 对基础数据做初步判断，输出结构化的"信号标签"
    - 阶段二（深度分析）：根据信号标签，动态选择额外数据源
  - **工具注册表**：将所有数据获取工具注册为工具池，每个工具附带"适用场景"标签
  - 分析师通过 LLM Function Calling 自主选择调用哪些工具
- **改动范围**：`tradingagents/agents/analysts/*.py` + 新增工具注册模块
- **风险**：增加一次 LLM 调用延迟，可能增加 Token 消耗
- **收益**：分析报告的针对性和深度显著提升
- **优先级**：P0

### 16. 方案 B：分析师间的条件通信

- **核心思想**：允许分析师在特定条件下向其他分析师"提问"或"请求验证"
- **现状问题**：7 名分析师完全独立运行，互不通信
- **方案设计**：
  - **信号置信度机制**：每个分析师输出报告时，同时输出 `confidence_signals`
  - 当某个信号的置信度低于阈值（如 0.6），系统自动触发跨域验证
  - **验证链示例**：
    ```
    基本面分析师发现"营收下滑30%"（置信度 0.5）
    → 自动触发新闻分析师验证："是否有重大利空事件？"
    → 新闻分析师回复："发现一则监管处罚公告"
    → 基本面分析师修正结论，提升置信度
    ```
  - 实现方式：通过 LangGraph 的 `conditional_edges` 条件分支实现
- **改动范围**：`tradingagents/graph/setup.py` 新增条件分支 + `conditional_logic.py`
- **风险**：可能增加整体分析时间 20-30%
- **收益**：显著降低"单一分析师误判"的风险
- **优先级**：P1

### 17. 方案 C：策略自适应（Context-Aware Strategy）

- **核心思想**：根据市场环境动态调整整个分析框架的侧重点
- **现状问题**：牛市、熊市、震荡市用同一套分析策略
- **方案设计**：
  - **市场环境分类器**（新增前置节点）：
    - 在分析师之前新增轻量级节点，快速判断当前市场环境
    - 输出分类：`bull_market` / `bear_market` / `sideways` / `volatile` / `event_driven`
    - 判断依据：大盘指数趋势、波动率水平、成交量变化
  - **环境驱动的 Prompt 注入**：
    - 熊市 → 基本面分析师增加"偿债能力"权重，风控辩论增加"下行风险"关注
    - 事件驱动 → 新闻分析师权重提升，技术分析师权重降低
  - **研究总监的权重调整**：根据市场环境调整各分析师报告的参考权重
- **改动范围**：新增环境分类器节点 + 修改各分析师 Prompt 模板 + 修改研究总监逻辑
- **风险**：分类器误判可能导致策略偏差
- **收益**：在极端市场环境下决策质量显著提升
- **优先级**：P1

---

## 四、系统性评估体系

### 18. 方案 D：预测准确性追踪系统

- **核心思想**：记录每个 Agent 的每一次预测，建立长期准确性档案
- **现状问题**：Reflector 只看盈亏，不看过程；无法区分"运气好"和"分析正确"
- **方案设计**：
  - **数据模型**：
    ```
    PredictionRecord:
      - agent_id          # 哪个 Agent
      - symbol            # 标的
      - trade_date        # 预测日期
      - prediction_type   # direction / price_range / risk_event
      - predicted_value   # 预测值
      - confidence        # 预测置信度 (0-1)
      - reasoning_hash    # 推理过程摘要哈希
      - actual_outcome    # 实际结果（延迟回填）
      - accuracy_score    # 准确性评分（延迟计算）
    ```
  - **核心指标**：
    - 方向准确率：预测方向正确的比例
    - 置信度校准度：置信度 80% 的预测实际准确率是否接近 80%
    - 信息增量：加入某 Agent 后整体准确率的提升
    - 一致性评分：同一标的不同次分析的结论一致性
    - 夏普比率：模拟交易收益的夏普比率
- **改动范围**：新增 `prediction_records` 数据表 + 修改所有分析师输出结构 + 新增回填和计算服务
- **风险**：需要积累足够多的数据才有统计意义
- **收益**：建立可量化的 Agent 能力画像，为动态权重调整提供数据基础
- **优先级**：P0（先行建立评估基线）

### 19. 方案 E：A/B 测试框架

- **核心思想**：支持对比不同配置下 Agent 系统的表现差异
- **测试场景**：
  - **Agent 消融测试**：关闭某个分析师，观察整体决策质量变化
  - **Prompt 版本测试**：同一分析师使用不同版本的 Prompt
  - **模型对比测试**：同一 Agent 使用不同 LLM（GPT-4o vs Claude-3.5-sonnet）
  - **辩论轮次测试**：不同的辩论轮次配置（1 轮 vs 2 轮 vs 3 轮）
- **数据模型**：
  ```
  ABTestExperiment:
    - experiment_id
    - name
    - control_config
    - treatment_config
    - symbols
    - date_range
    - status
    - results

  ABTestResult:
    - experiment_id
    - group
    - symbol
    - trade_date
    - prediction
    - actual_outcome
    - accuracy_score
    - token_cost
    - latency_ms
  ```
- **改动范围**：新增实验管理模块 + 修改 `TradingAgentsGraph` 支持配置注入 + 前端实验管理页面
- **风险**：实现复杂度较高，需要回测数据支持
- **收益**：实现科学化的 Agent 调优
- **优先级**：P2

### 20. 方案 F：决策归因分析

- **核心思想**：当一次分析结果与实际走势严重偏离时，自动定位问题出在哪个环节
- **归因链路**：
  ```
  最终决策错误
    → 是研究总监裁决有误？
      → 是多头/空头研究员论证有误？
        → 是哪个分析师的数据/结论有误？
          → 是数据源问题还是推理问题？
  ```
- **方案设计**：
  - **事后归因报告**：在实际结果出来后（T+1 或 T+5），自动触发归因分析
  - **错误模式识别**：积累多次归因数据后，识别系统性错误模式
  - 例如："技术分析师在高波动环境下经常误判突破" → 自动调整高波动期的技术分析权重
- **改动范围**：新增归因分析模块 + 修改 Reflector 逻辑
- **风险**：归因逻辑的准确性依赖于中间结果的结构化程度
- **收益**：从"事后反思"升级为"精准归因"，加速 Agent 系统的迭代优化
- **优先级**：P2

---

## 五、实施路线图

| 阶段 | 编号 | 方案 | 依赖 | 预期收益 | 优先级 |
|------|------|------|------|---------|--------|
| Phase 1 | 18 | D. 预测准确性追踪 | 无 | 建立评估基线 | P0 |
| Phase 1 | 15 | A. 动态工具选择 | 无 | 分析深度提升 | P0 |
| Phase 1 | 9 | 持仓预警系统 | 无 | 用户刚需 | P0 |
| Phase 1 | 10 | 持仓对比分析 | 无 | 与分析系统深度结合 | P0 |
| Phase 1 | 6 | 全局 Toast 通知 | 无 | 统一交互体验 | P1 |
| Phase 1 | 1 | 持仓排序与筛选 | 无 | 提升日常体验 | P1 |
| Phase 1 | 2 | 批量操作 | 无 | 效率提升 | P1 |
| Phase 1 | 4 | 导入格式兼容性 | 无 | 降低使用门槛 | P1 |
| Phase 2 | 17 | C. 策略自适应 | D 的数据 | 极端市场决策提升 | P1 |
| Phase 2 | 16 | B. 分析师条件通信 | D 的数据 | 降低误判风险 | P1 |
| Phase 2 | 5 | 控制台盈亏汇总 | 无 | 信息展示优化 | P2 |
| Phase 2 | 3 | 行情状态指示 | 无 | 体验优化 | P2 |
| Phase 2 | 13 | 交易日历集成 | 无 | 功能完善 | P2 |
| Phase 2 | 11 | 持仓历史快照 | 无 | 数据积累 | P2 |
| Phase 2 | 12 | 分析结果对比 | 无 | 数据价值挖掘 | P2 |
| Phase 3 | 20 | F. 决策归因分析 | D 的数据积累 | 精准定位问题 | P2 |
| Phase 3 | 19 | E. A/B 测试框架 | D + F | 科学化调优 | P2 |
| Phase 3 | 14 | 持仓再平衡建议 | 无 | 算法复杂度高 | P3 |
| Phase 3 | 7 | 虚拟列表 | 无 | 持仓多时才有收益 | P3 |
| Phase 3 | 8 | 乐观更新 | 无 | 体验优化 | P3 |
