# TradingAgents-AShare 自进化系统设计方案

> 从系统论、控制论和进化论的角度，设计一个让 Agent 系统具备"自我改进"能力的完整方案。

---

## 一、问题定义：什么是"自进化"？

### 1.1 什么是自进化系统

一个自进化系统是指：**系统在运行过程中，能够基于自身行为的结果反馈，自主地调整内部结构和策略，使得未来的行为表现持续改善，且这一过程不需要人类显式地重新编程。**

对于 TradingAgents-AShare 而言，自进化的含义是：
- 系统做出了一个"买入"建议，一周后股价涨了 → 系统应该记住这次成功的模式
- 系统做出了一个"买入"建议，一周后股价跌了 → 系统应该分析失败原因并调整策略
- 系统发现"技术分析师在高波动环境下经常误判" → 系统应该自动降低该场景下技术分析的权重

### 1.2 当前系统的反馈闭环现状

通过代码审查，当前系统存在一个**断裂的反馈链**：

```
当前状态：
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│ 数据采集  │ ──→ │ Agent 分析│ ──→ │ 输出决策  │ ──→ │ ？？？   │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
                                                       ↓
                                              实际市场走势（未被采集）
                                                       ↓
                                              Reflector（仅简单盈亏对比）
                                                       ↓
                                              BM25 记忆（无容量控制、无结构化归因）
```

**断裂点**：
1. **结果回填缺失**：系统输出决策后，没有自动采集实际市场走势来验证决策质量
2. **归因分析缺失**：Reflector 只看"赚了还是亏了"，不分析"是哪个环节的判断出了问题"
3. **记忆质量失控**：BM25 记忆只增不减，过时记忆会干扰当前决策
4. **策略调整缺失**：即使发现了系统性错误模式，也没有机制自动调整策略

### 1.3 自进化的边界

**重要声明**：自进化 ≠ 自主交易。本方案不涉及让系统自主执行交易，而是让系统的**分析和建议能力**持续改善。所有进化结果都需要人类审核和确认。

---

## 二、自进化的五层模型

借鉴生物进化论和控制论，我们将自进化分为五个层次，从低到高：

```
┌─────────────────────────────────────────────────────────────┐
│  L5  元进化（Meta-Evolution）                                 │
│  进化机制本身的进化：优化进化算法的参数和策略                      │
├─────────────────────────────────────────────────────────────┤
│  L4  架构进化（Architecture Evolution）                        │
│  系统结构的自适应：Agent 拓扑、权重、角色的动态调整                │
├─────────────────────────────────────────────────────────────┤
│  L3  策略进化（Strategy Evolution）                            │
│  分析策略的自适应：Prompt、工具选择、分析框架的优化                 │
├─────────────────────────────────────────────────────────────┤
│  L2  记忆进化（Memory Evolution）                              │
│  经验的结构化积累：记忆的筛选、归因、淘汰、重组                     │
├─────────────────────────────────────────────────────────────┤
│  L1  感知进化（Perception Evolution）                          │
│  数据采集的自适应：根据分析需求动态调整数据源和采集策略               │
└─────────────────────────────────────────────────────────────┘
```

每一层的进化都依赖下一层提供的能力。下面逐一设计。

---

## 三、L1 感知进化：自适应数据采集

### 3.1 核心思想

当前系统的数据采集是**静态的**：每个分析师固定获取预定义的数据字段。感知进化的意思是：**系统根据当前分析任务的特点，动态决定需要采集哪些数据、从哪些数据源采集。**

### 3.2 设计方案

#### 3.2.1 数据价值评估器

每次分析完成后，系统回顾：哪些被采集的数据对最终决策产生了实质性影响？

```
数据价值评估流程：
1. 分析师报告中引用了哪些数据点？（通过报告文本与数据的关联分析）
2. 这些数据点是否影响了最终决策方向？（对比有/无该数据的决策差异）
3. 长期积累后，哪些数据源的"信息增量"最高？
```

#### 3.2.2 动态数据源选择

基于数据价值评估结果，系统可以：
- **高频使用且高信息增量的数据**：优先采集，增加采集频率
- **低频使用或低信息增量的数据**：降低采集优先级，在 LLM Token 预算紧张时首先裁剪
- **新出现的异常数据**：自动触发补充分析（如突然出现大量龙虎榜数据）

#### 3.2.3 数据质量感知

系统需要感知数据质量：
- 数据延迟：实时数据 vs T+1 数据
- 数据完整性：某些字段缺失
- 数据异常值：突变数据是否需要验证

### 3.3 可行性评估

| 维度 | 评估 |
|------|------|
| 技术可行性 | ⭐⭐⭐⭐ 高。数据采集层已有抽象（`interface.py`），只需在上层增加价值评估 |
| 实现复杂度 | 中。需要建立"数据 → 报告 → 决策"的归因链路 |
| 风险 | 低。最坏情况是数据采集策略不改善，不会恶化 |
| 预期收益 | 中。减少无效数据采集，提升分析效率 |

---

## 四、L2 记忆进化：结构化经验积累

### 4.1 核心问题

当前记忆系统（[memory.py](file:///d:/workspace/github/TradingAgents-AShare/tradingagents/agents/utils/memory.py)）的根本问题是：**记忆是"只增不删的文本流"，缺乏结构化和质量管理。**

具体问题：
1. **无容量上限**：记忆会无限膨胀
2. **无淘汰机制**：过时的市场记忆（如"2024年牛市经验"）会干扰2026年的分析
3. **无结构化**：记忆是自由文本，无法做精细的归因和检索
4. **无验证机制**：Reflector 写入的记忆质量参差不齐，可能是"错误的反思"

### 4.2 设计方案：记忆生命周期管理

```
记忆生命周期：
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  产生     │ ──→ │  验证     │ ──→ │  成熟     │ ──→ │  淘汰     │
│ (Reflector│     │ (结果回填  │     │ (多次被引用│     │ (过时/失效 │
│  写入)    │     │  后验证)   │     │  且有效)   │     │  被遗忘)   │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
```

#### 4.2.1 结构化记忆条目

将记忆从自由文本升级为结构化条目：

```
MemoryEntry:
  id                    # 唯一标识
  agent_id              # 产生该记忆的 Agent
  symbol                # 关联的标的
  market_regime         # 市场环境（bull/bear/sideways/volatile）
  situation_summary     # 情境摘要（用于 BM25 检索）
  decision              # 当时的决策
  outcome               # 实际结果（延迟回填）
  accuracy_score        # 准确性评分
  lesson                # 提炼的经验教训
  confidence            # 记忆的可信度（初始低，验证后提高）
  citation_count        # 被引用次数
  last_cited_at         # 最后引用时间
  created_at            # 创建时间
  expires_at            # 过期时间（可续期）
  status                # pending / verified / mature / expired
```

#### 4.2.2 记忆验证机制

Reflector 写入的记忆初始状态为 `pending`，需要经过验证才能变为 `verified`：

```
验证流程：
1. 记忆写入时，记录关联的标的和决策
2. T+1/T+5 后，自动采集实际市场走势
3. 对比"记忆中的经验教训"与"实际情况"
4. 如果一致：confidence += 0.2，citation_count += 1
5. 如果矛盾：confidence -= 0.3，如果 confidence < 0.2 则标记为 expired
6. 验证通过（confidence > 0.5）的状态变为 verified
```

#### 4.2.3 记忆淘汰策略

```
淘汰规则（按优先级）：
1. 硬淘汰：expires_at < now → 直接删除
2. 软淘汰：confidence < 0.2 → 标记为 expired，不再参与检索
3. 衰减淘汰：citation_count = 0 且 last_cited_at < 90天前 → 降权
4. 容量淘汰：超过 max_entries 时，按 confidence * recency_score 排序，淘汰末尾 20%
```

#### 4.2.4 记忆重组

定期（如每周）执行记忆重组：
- 合并相似情境的记忆（通过 embedding 相似度）
- 提炼高频模式为"元记忆"（如"高波动环境下技术分析易误判"）
- 清理冗余记忆

### 4.3 可行性评估

| 维度 | 评估 |
|------|------|
| 技术可行性 | ⭐⭐⭐⭐⭐ 高。BM25 记忆系统已有基础，结构化扩展容易 |
| 实现复杂度 | 中。需要结果回填机制和记忆状态管理 |
| 风险 | 低。记忆验证失败不会影响正常分析流程 |
| 预期收益 | 高。记忆质量直接影响所有 Agent 的决策质量 |

---

## 五、L3 策略进化：自适应分析策略

### 5.1 核心思想

策略进化是指：**系统根据历史表现，自动调整分析策略的参数和方法。** 这是自进化的核心层。

### 5.2 设计方案

#### 5.2.1 策略参数化

将当前硬编码的分析策略转化为可调参数：

```
StrategyConfig:
  # 分析师权重（研究总监裁决时的参考权重）
  analyst_weights:
    market: 1.0
    fundamentals: 1.0
    news: 1.0
    sentiment: 1.0
    macro: 1.0
    smart_money: 1.0
    volume_price: 1.0

  # 辩论参数
  debate_params:
    max_debate_rounds: 2
    claim_resolution_threshold: 0.7  # Claim 解决的置信度阈值
    focus_claim_weight: 1.5          # 聚焦 Claim 的权重倍数

  # 风控参数
  risk_params:
    max_risk_discuss_rounds: 1
    revision_threshold: 0.6          # 触发修订的风控评分阈值
    max_retries: 2

  # 分析师 Prompt 版本
  prompt_versions:
    market_analyst: "v2.1"
    fundamentals_analyst: "v1.3"
    # ...

  # 工具选择策略
  tool_selection:
    mode: "static"  # static / adaptive
    fallback_strategy: "conservative"
```

#### 5.2.2 策略评估器

每次分析完成后，评估当前策略的有效性：

```
策略评估维度：
1. 方向准确率：预测方向 vs 实际走势
2. 置信度校准度：预测置信度与实际准确率的吻合度
3. 信息效率：从数据到决策的信息损失
4. 一致性：同一标的多次分析的结论稳定性
5. 风险调整收益：模拟交易的夏普比率
```

#### 5.2.3 策略优化器

基于策略评估结果，自动调整策略参数：

**方式一：基于规则的调整（短期可行）**
```
规则示例：
IF 技术分析师在高波动环境下准确率 < 40%
THEN 降低高波动环境下 market_analyst 的权重至 0.6

IF 新闻分析师在事件驱动型市场中准确率 > 70%
THEN 提升事件驱动环境下 news_analyst 的权重至 1.5

IF 辩论轮次从 2 增加到 3 后，决策准确率提升 > 5%
THEN 将 max_debate_rounds 设为 3
```

**方式二：基于贝叶斯优化的参数搜索（中期目标）**
```
目标函数：maximize(方向准确率 * 置信度校准度 - Token成本 * λ)
搜索空间：analyst_weights 的 7 维空间 + debate_params 的 3 维空间
方法：贝叶斯优化（如 Optuna），每次分析后更新后验分布
```

**方式三：基于进化算法的策略搜索（长期目标）**
```
种群：一组 StrategyConfig
适应度函数：综合评估指标（准确率 + 风控评分 + 成本效率）
选择：保留 Top-K 策略
交叉：混合不同策略的参数
变异：随机扰动参数
代际：每积累 100 次分析结果为一代
```

#### 5.2.4 市场环境感知

策略调整需要考虑市场环境：

```
市场环境分类器：
输入：大盘指数趋势、波动率水平、成交量变化、板块轮动
输出：bull_market / bear_market / sideways / volatile / event_driven

每个市场环境维护独立的 StrategyConfig
系统运行时自动识别当前环境，加载对应策略
```

### 5.3 可行性评估

| 维度 | 评估 |
|------|------|
| 技术可行性 | ⭐⭐⭐⭐ 高。基于规则的调整可立即实现，贝叶斯优化和进化算法需要数据积累 |
| 实现复杂度 | 高。需要完整的结果回填、归因分析、参数管理系统 |
| 风险 | 中。策略调整可能导致短期内决策质量波动 |
| 预期收益 | 高。这是自进化的核心价值所在 |

---

## 六、L4 架构进化：自适应系统拓扑

### 6.1 核心思想

架构进化是指：**系统能够根据任务特点和性能表现，动态调整 Agent 的拓扑结构。** 这是最高级的进化形式。

### 6.2 设计方案

#### 6.2.1 动态 Agent 激活

不是所有分析任务都需要全部 7 名分析师。系统可以根据任务特点，动态选择激活哪些 Agent：

```
Agent 选择策略：
- 小盘股短线分析：重点激活 技术分析师 + 主力资金分析师 + 量价分析师
- 大盘蓝筹中线分析：重点激活 基本面分析师 + 宏观分析师 + 新闻分析师
- 事件驱动型分析：重点激活 新闻分析师 + 舆情分析师 + 技术分析师
```

#### 6.2.2 动态辩论深度

辩论轮次不应该是固定的，而应该根据分歧程度动态调整：

```
动态辩论深度：
1. 第 1 轮：多空各发言 1 次
2. 检查 Claim 解决率：
   - 如果 > 70% 的 Claim 已解决 → 结束辩论
   - 如果 < 70% → 继续第 2 轮
3. 第 2 轮：聚焦未解决的 Claim
4. 再次检查：
   - 如果 > 85% 已解决 → 结束
   - 如果 < 85% 且 < max_rounds → 继续
5. 达到 max_rounds 后强制结束，研究总监裁决未解决的 Claim
```

#### 6.2.3 动态风控参与

风控辩论的深度应该与交易方案的风险等级挂钩：

```
风控参与策略：
- 低风险方案（如"持有"）：风控各辩手发言 1 轮即可
- 中风险方案（如"小仓位买入"）：标准风控流程（1-2 轮）
- 高风险方案（如"大仓位买入"或"清仓"）：加深风控辩论（2-3 轮），增加额外的风控检查点
```

#### 6.2.4 Agent 角色演化

长期来看，系统可以自动生成新的 Agent 角色：

```
角色演化流程：
1. 识别分析盲区：某些类型的分析任务持续表现不佳
2. 生成候选角色：基于盲区描述，LLM 生成新 Agent 的角色定义
3. 小规模测试：在历史数据上回测新 Agent 的贡献
4. 渐进集成：先作为"观察者"角色参与，不影响决策，只记录其判断
5. 验证有效后：正式集成到决策流程中
```

### 6.3 可行性评估

| 维度 | 评估 |
|------|------|
| 技术可行性 | ⭐⭐⭐ 中。动态激活和动态深度容易实现，角色演化难度极高 |
| 实现复杂度 | 高。需要修改 LangGraph 的图结构，支持动态节点和边 |
| 风险 | 中高。架构变更可能导致系统不稳定 |
| 预期收益 | 中。主要是效率提升和极端场景的决策质量改善 |

---

## 七、L5 元进化：进化机制的自我优化

### 7.1 核心思想

元进化是指：**进化机制本身的参数和策略也可以被优化。** 例如：
- 记忆淘汰的阈值是否合理？
- 策略优化的搜索空间是否需要调整？
- 数据价值评估的指标是否有效？

### 7.2 设计方案

#### 7.2.1 进化效果追踪

```
EvolutionMetrics:
  - memory_quality_trend     # 记忆质量趋势（验证通过率）
  - strategy_improvement     # 策略改善幅度（准确率变化）
  - adaptation_speed         # 适应速度（从环境变化到策略调整的延迟）
  - evolution_cost           # 进化成本（额外的 Token 消耗和延迟）
  - stability_score          # 稳定性评分（进化前后决策一致性）
```

#### 7.2.2 进化参数自动调整

```
元进化规则：
IF 记忆验证通过率 < 30% → 降低 Reflector 的写入频率，提高验证标准
IF 策略优化后准确率反而下降 → 回滚到上一版本，扩大搜索空间
IF 适应速度过慢（> 100 次分析才调整）→ 提高策略评估频率
IF 进化成本过高（Token 消耗增加 > 20%）→ 简化进化流程
```

#### 7.2.3 进化日志与审计

所有进化操作都需要记录，支持回溯和审计：

```
EvolutionLog:
  - timestamp
  - evolution_layer    # L1-L5
  - trigger            # 触发原因
  - before_state       # 进化前状态
  - after_state        # 进化后状态
  - expected_impact    # 预期影响
  - actual_impact      # 实际影响（延迟回填）
  - rollback_available # 是否可回滚
```

### 7.3 可行性评估

| 维度 | 评估 |
|------|------|
| 技术可行性 | ⭐⭐⭐ 中。需要 L1-L4 的基础设施完善后才能有效运行 |
| 实现复杂度 | 高。元进化系统本身需要高度的抽象能力 |
| 风险 | 中。过度优化可能导致系统行为不可预测 |
| 预期收益 | 长期高。确保进化方向的正确性 |

---

## 八、结果回填机制：自进化的基石

### 8.1 为什么结果回填是基石

所有层次的进化都依赖一个共同的基础：**知道"决策是否正确"。** 没有结果回填，自进化就是无根之木。

### 8.2 设计方案

#### 8.2.1 预测记录

每次分析完成后，自动记录预测快照：

```
PredictionSnapshot:
  - prediction_id
  - symbol
  - trade_date
  - direction          # BUY / SELL / HOLD
  - confidence         # 置信度
  - target_price       # 目标价
  - stop_loss          # 止损价
  - reasoning_summary  # 推理摘要
  - analyst_traces     # 各分析师的判断
  - debate_claims      # 辩论 Claim 状态
  - risk_verdict       # 风控裁决
  - strategy_config    # 当时使用的策略配置
  - created_at
```

#### 8.2.2 结果采集

```
结果采集流程：
1. 分析完成后，记录预测快照
2. T+1（次日）：采集次日收盘价，计算短期收益
3. T+5（一周后）：采集一周收益，计算中期收益
4. T+20（一月后）：采集一月收益，计算长期收益
5. 将实际结果回填到 PredictionSnapshot
```

#### 8.2.3 归因分析

结果回填后，自动执行归因分析：

```
归因分析流程：
1. 如果预测正确：
   - 是哪些分析师的判断最准确？（信息增量分析）
   - 是多头还是空头研究员的论证更有说服力？
   - 风控裁决是否正确放行？
   
2. 如果预测错误：
   - 错误出在哪个环节？
     a. 数据层面：数据源有问题？数据延迟？
     b. 分析层面：哪个分析师的判断偏差最大？
     c. 辩论层面：Claim 是否被正确追踪？
     d. 决策层面：研究总监是否忽略了关键反对意见？
     e. 风控层面：风控是否应该拦截？
   - 错误模式是什么？（如：高波动环境下技术分析误判）
```

### 8.3 可行性评估

| 维度 | 评估 |
|------|------|
| 技术可行性 | ⭐⭐⭐⭐⭐ 高。AKShare 已支持历史行情查询 |
| 实现复杂度 | 中。需要新增定时任务和归因分析模块 |
| 风险 | 低。结果回填是被动操作，不影响正常分析 |
| 预期收益 | 极高。这是整个自进化系统的数据基础 |

---

## 九、实施路线图

### Phase 0：基础设施（2 周）

**目标**：建立自进化的数据基础

| 任务 | 优先级 | 复杂度 |
|------|--------|--------|
| 预测快照记录 | P0 | 低 |
| 结果回填机制（T+1/T+5/T+20） | P0 | 中 |
| 基础归因分析（正确/错误 + 环节定位） | P0 | 中 |
| 进化日志表 | P0 | 低 |

### Phase 1：记忆进化（2-3 周）

**目标**：让记忆系统从"只增不删"升级为"生命周期管理"

| 任务 | 优先级 | 复杂度 |
|------|--------|--------|
| 结构化记忆条目 | P0 | 中 |
| 记忆验证机制 | P0 | 中 |
| 记忆容量控制和淘汰策略 | P0 | 低 |
| 记忆质量仪表盘（前端） | P1 | 低 |

### Phase 2：策略进化 — 规则驱动（3-4 周）

**目标**：基于规则的策略自动调整

| 任务 | 优先级 | 复杂度 |
|------|--------|--------|
| 策略参数化 | P0 | 中 |
| 市场环境分类器 | P0 | 中 |
| 基于规则的分析师权重调整 | P0 | 中 |
| 动态辩论深度 | P1 | 中 |
| 策略版本管理和回滚 | P1 | 中 |
| 策略进化仪表盘（前端） | P1 | 中 |

### Phase 3：策略进化 — 数据驱动（4-6 周）

**目标**：基于统计和优化算法的策略自动调整

| 任务 | 优先级 | 复杂度 |
|------|--------|--------|
| 贝叶斯优化参数搜索 | P1 | 高 |
| 数据价值评估器 | P1 | 中 |
| 动态 Agent 激活 | P1 | 高 |
| 动态风控参与深度 | P2 | 中 |

### Phase 4：元进化（长期）

**目标**：进化机制的自我优化

| 任务 | 优先级 | 复杂度 |
|------|--------|--------|
| 进化效果追踪 | P2 | 中 |
| 进化参数自动调整 | P2 | 高 |
| 进化算法策略搜索 | P3 | 极高 |
| Agent 角色自动生成 | P3 | 极高 |

---

## 十、风险评估与缓解

### 10.1 风险矩阵

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| 进化导致决策质量短期下降 | 中 | 高 | 所有进化操作可回滚；设置"进化冻结期" |
| 过度优化（Overfitting） | 中 | 高 | 使用交叉验证；限制进化幅度 |
| 进化成本过高 | 低 | 中 | 设置 Token 预算上限；优先低成本进化 |
| 系统行为不可预测 | 低 | 高 | 完整的进化日志；人类审核关键进化决策 |
| 记忆验证数据不足 | 中 | 中 | 初期使用历史数据回测补充 |

### 10.2 安全机制

```
安全机制：
1. 进化幅度限制：单次进化不允许参数变化超过 ±30%
2. 回滚机制：所有进化操作保留 before_state，可一键回滚
3. 人类审核：关键进化决策（如新增 Agent）需要人类确认
4. 进化冻结期：重大市场事件期间（如财报季）暂停进化
5. A/B 测试：新策略先在 10% 的任务上试运行
```

---

## 十一、关键设计原则

### 11.1 渐进式进化

**原则**：不要试图一步到位实现所有层次的进化。从 L1（感知进化）和 L2（记忆进化）开始，逐步向上。

**原因**：每一层的进化都依赖下层提供的能力。没有可靠的结果回填（L1/L2 基础），策略进化（L3）就是空中楼阁。

### 11.2 可观测性优先

**原则**：在实现任何进化机制之前，先建立完整的可观测性。

**原因**：你无法优化你无法衡量的东西。预测记录、结果回填、归因分析是所有进化的前提。

### 11.3 保守进化

**原则**：宁可进化慢一点，也不要引入不稳定的进化。

**原因**：在金融分析领域，错误的进化比不进化更危险。所有进化结果都需要验证后才能生效。

### 11.4 人类在环

**原则**：自进化不等于无人监督。关键进化决策需要人类审核。

**原因**：LLM 的判断可能有偏差，进化方向可能偏离人类的预期。保持人类的最终决策权。

---

## 十二、与现有系统的集成点

| 现有模块 | 集成方式 | 改动量 |
|---------|---------|--------|
| `Reflector` | 扩展为结构化归因分析，写入结构化记忆 | 中 |
| `FinancialSituationMemory` | 升级为支持生命周期管理的记忆系统 | 中 |
| `ConditionalLogic` | 增加动态辩论深度的判断逻辑 | 小 |
| `GraphSetup` | 支持动态 Agent 激活 | 大 |
| `DataCollector` | 增加数据价值评估和动态选择 | 中 |
| `api/main.py` | 新增预测快照、结果回填、进化日志的 API | 中 |
| 前端 | 新增进化仪表盘页面 | 中 |

---

## 十三、总结

TradingAgents-AShare 的自进化不是一个单一功能，而是一个**分层递进的系统工程**：

```
                    ┌─────────────┐
                    │   L5 元进化  │  ← 进化机制的自我优化
                    ├─────────────┤
                    │  L4 架构进化  │  ← Agent 拓扑动态调整
                    ├─────────────┤
                    │  L3 策略进化  │  ← 分析策略自适应
                    ├─────────────┤
                    │  L2 记忆进化  │  ← 经验结构化积累
                    ├─────────────┤
                    │  L1 感知进化  │  ← 数据采集自适应
                    └─────────────┘
                         ↑
                    ┌─────────────┐
                    │  结果回填机制  │  ← 所有进化的基石
                    └─────────────┘
```

**核心洞察**：自进化的关键不是"让 LLM 更聪明"，而是**建立从"决策 → 结果 → 归因 → 改进"的完整闭环**。当前系统最大的断裂点在于"结果"这一环——系统做出了决策，但不知道决策是否正确。补上这一环，自进化就有了根基。
